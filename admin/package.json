{
  "name": "cubeplane-admin",
  "version": "0.1.0",
  "description": "Atlas-powered admin/control panel for the cubeplane Minecraft server",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "atlas": "github:wess/atlas"
  },
  "devDependencies": {
    "typescript": "^5",
    "bun-types": "latest"
  }
}
