# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# cubeplane runtime data
/world_data
/logs
*.log

# Bedrock bridge (ViaProxy + Geyser) — jars & runtime are downloaded by run.sh
/bridge

# Admin panel (Bun/Node)
admin/node_modules
admin/dist
admin/.env
admin/bun.lockb.orig

# Editor / OS
.DS_Store
.idea
.vscode
*.swp
